{
  "name": "roboflow-filter-backend",
  "version": "1.0.0",
  "description": "Backend API for Roboflow image metadata filtering",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test server.test.js",
    "lint": "eslint *.js --max-warnings 0"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
